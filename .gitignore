# Build output: never commit. Everything here is regenerated by make,
# build/*.sh or scripts/pack-ipk.py.

# Built Firefox runtime and native binaries
app/firefox-runtime/
app/firefox
app/lib/
app/smoke
app/geckotv

# Packages
dist/
*.ipk

# Firmware libraries copied from the TV (LG's binaries, not ours)
sysroot/

# Build trees, generated code and logs
build/firefox-src/
build/gtk-build/
build/gen/
build/configure-help.txt
build/linux-build.log
build/eglprobe*
*.o
*.so
*.so.*
__pycache__/

# Runtime state and one-off debug switches on the TV side
app/profile/
app/*.log
app/env
app/wayland-debug
app/gl-bridge
*.moz_log
*.bak
*.bak-*

.DS_Store
